
/*
* create a header to display the Theme context
  - header needs an h1, button to toggle the theme for the rest of the app
* create a TodoForm and TodoList complete to show CRUD functionality
  - TodoForm: you need a form, label, input field, button to add a todo
  - TodoList: You need to be able to read, complete, update and delete to dos from a list
* */

const App = () => {

  return (
      <div style={{ margin: '0 auto', maxWidth: '600px' }}>
        <h2>Todo List</h2>
      </div>
  );
};

export default App;
